const initialState = {
  count: 0,
  offset: 0,
  loading: false,
  listOfPokemon: [],
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
