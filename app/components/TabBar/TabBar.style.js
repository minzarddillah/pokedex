import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 10,
  },
  title: {
    textAlign: 'center',
    marginBottom: 10,
  },
  border: {
    borderWidth: 1,
    width: '100%',
  },
});

export default styles;
