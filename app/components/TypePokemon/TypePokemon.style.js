import {StyleSheet} from 'react-native';
import color from '../../utils/color';

const styles = StyleSheet.create({
  containerType: {
    marginHorizontal: 10,
    paddingHorizontal: 15,
    paddingVertical: 7,
    borderRadius: 30,
    backgroundColor: color.TropicalBlue,
  },
});

export default styles;
